CREATE TABLE IF NOT EXISTS prospect (
    uuid bigint PRIMARY KEY,
    userid bigint,

    sapphire bigint,
    amber bigint,
    citrine bigint,
    ruby bigint,
    jade bigint,
    amethyst bigint
) 