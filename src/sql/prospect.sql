CREATE TABLE IF NOT EXISTS prospect (
    uuid bigint PRIMARY KEY AUTO_INCREMENT,
    userid bigint,

    sapphire bigint,
    amber bigint,
    citrine bigint,
    ruby bigint,
    jade bigint,
    amethyst bigint
) 